export class AppPages {
  // Базовый корень приложения
  static ROOT = "/";

  // Авторизация
  static LOGIN = "/login";

  // Основные страницы
  static HOME = AppPages.ROOT;
  static CLIENTS = "/clients";
  static RESERVATIONS = "/reservations";
  static TASKS = "/tasks";
}
